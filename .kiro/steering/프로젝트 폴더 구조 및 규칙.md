🏗️ Project Folder Structure & Conventions

이 문서는 프로젝트의 디렉토리 구조와 파일 네이밍 규칙을 정의합니다.
개발 시 이 규칙을 준수하여 일관성을 유지해주세요.

## 1. 📂 전체 디렉토리 트리 (Directory Tree)

```
root/
├── client/                      # React Native 클라이언트
│   ├── App.js                   # 앱 진입점 (Provider 설정)
│   ├── global.css               # NativeWind 전역 스타일 (@apply 정의)
│   │
│   └── src/                     # 메인 소스 코드
│       ├── api/                 # 서버 통신 로직 (Axios)
│       ├── components/          # 리액트 컴포넌트
│       │   ├── ui/              # 재사용 가능한 공통 UI (Button, Input...)
│       │   ├── domain/          # 특정 도메인 종속 컴포넌트 (TodoList...)
│       │   └── layout/          # 화면 레이아웃 래퍼 (Container...)
│       ├── constants/           # 상수 데이터 (Colors, Strings...)
│       ├── hooks/               # 커스텀 훅 (useTodos, useAuth...)
│       ├── navigation/          # React Navigation 설정
│       │   └── MainTabs.js      # 하단 탭 네비게이션
│       ├── screens/             # 화면 컴포넌트 (LoginScreen, TodoScreen...)
│       ├── store/               # 전역 상태 관리 (Zustand)
│       └── utils/               # 순수 유틸리티 함수 (Date formatting...)
│
└── server/                      # 백엔드 (Node.js + Express)
    ├── config/                  # DB 연결, 환경변수 설정
    ├── controllers/             # 요청 처리 로직
    ├── models/                  # Mongoose 스키마 (Schema)
    ├── routes/                  # API 라우트 정의
    └── middlewares/             # 인증, 로깅 미들웨어
``` ## 2. 📁 상세 폴더 역할 가이드

### src/screens/ (화면)
- **역할**: 실제 화면(Screen)이 렌더링되는 곳입니다.
- **규칙**:
  - 로직을 최소화하고, 주로 컴포넌트 조립과 데이터 호출(Query)만 담당합니다.
  - 파일명은 `ScreenName + Screen.js` 형식 (예: `LoginScreen.js`, `TodoScreen.js`)

### src/navigation/ (네비게이션)
- **역할**: React Navigation 설정 및 라우팅 구조를 정의합니다.
- **예시**: `MainTabs.js` (하단 탭 네비게이션)

### src/components/ui/ (공통 UI 컴포넌트)
- **역할**: 프로젝트 전체에서 쓰이는 가장 작은 단위의 디자인 요소입니다.
- **특징**: 비즈니스 로직(Zustand, API 호출)이 절대 포함되면 안 됩니다. 오직 props로만 동작해야 합니다.
- **예시**: `Button.js`, `Input.js`, `Checkbox.js`

### src/components/domain/ (도메인 컴포넌트)
- **역할**: 특정 화면이나 기능에 종속된 컴포넌트입니다.
- **구조**: 도메인별로 폴더를 한 번 더 나눕니다.
  - `todo/`: `TodoList.js`, `TodoItem.js`
  - `notification/`: `NotificationCard.js`
- **특징**: 여기서 API 데이터를 받거나 Store에 접근해도 됩니다.

### src/hooks/
- **역할**: 로직을 재사용하기 위한 커스텀 훅입니다.
- **구분**:
  - `queries/`: TanStack Query 관련 훅 (`useTodos.js`, `useAuth.js`)
  - `common/`: 일반 로직 훅 (`useDebounce.js`)## 3. 📏 네이밍 컨벤션 (Naming Rules)

### 파일 및 폴더 (Files & Folders)

**컴포넌트 파일**: PascalCase (대문자 시작)
- ✅ `TodoItem.js`, `LoginScreen.js`
- ❌ `todoItem.js`, `login-screen.js`

**일반 JS 파일 (Hooks, Utils)**: camelCase (소문자 시작)
- ✅ `useTodos.js`, `dateFormatter.js`
- ❌ `UseTodos.js`, `DateFormatter.js`

**폴더명**: 소문자 (그룹핑 목적)
- ✅ `components/ui/`, `domain/todo/`
- ❌ `Components/UI/`

### 코드 내부 (Code)

**컴포넌트 이름**: 파일명과 동일하게 PascalCase
```javascript
// TodoItem.js
export default function TodoItem() { ... }
```

**상수(Constants)**: 대문자 스네이크 케이스
```javascript
const API_BASE_URL = "http://localhost:5000";
const MAX_TODO_LENGTH = 100;
```

**함수/변수**: camelCase
```javascript
const handleSubmit = () => { ... };
const todoList = [];
```## 4. 🎨 스타일링 규칙 (NativeWind v4)

### global.css 활용
복잡하거나 자주 쓰이는 스타일은 CSS 클래스로 정의하고 `@apply`를 사용합니다.

```css
/* global.css 예시 */
@layer components {
  .btn-primary {
    @apply bg-blue-500 py-3 px-4 rounded-lg active:opacity-80;
  }
  .text-header {
    @apply text-xl font-bold text-gray-900 dark:text-white;
  }
}
```

### 인라인 스타일링
일회성 스타일은 JSX 내에서 `className`을 사용합니다.

```javascript
<View className="flex-1 bg-white dark:bg-black p-4">
  <Text className="text-lg font-bold">Hello</Text>
</View>
``` ## 5. 💾 서버 폴더 구조 (Backend)

`server/` 폴더 내부는 MVC 패턴과 유사하게 가져갑니다.

- **models/**: 데이터베이스 스키마 정의 (`Todo.js`, `User.js`)
- **controllers/**: 실제 로직 (`getTodos`, `createTodo`)
- **routes/**: URL 주소와 컨트롤러 연결 (`todoRoutes.js`)

```javascript
// 예시: routes/todoRoutes.js
router.get('/', todoController.getTodos);
router.post('/', todoController.createTodo);
```

## ✅ 체크리스트

- [ ] 새로운 컴포넌트를 만들 때 `ui`인지 `domain`인지 고민했는가?
- [ ] 컴포넌트 파일명은 대문자로 시작했는가? (PascalCase)
- [ ] 비즈니스 로직은 컴포넌트에서 분리해 `hooks`로 뺐는가?
- [ ] 스타일이 복잡해지면 `global.css`로 추출했는가?
- [ ] Screen 파일은 `screens/` 폴더에 `ScreenName + Screen.js` 형식으로 작성했는가?
