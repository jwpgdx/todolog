# Todo Screen V2 Raw Memo

Last Updated: 2026-05-18
Status: Raw memo, not frozen

이 문서는 Todo Screen v2에 대해 사용자가 한 번에 적은 아이디어/고민/질문을 원문에 가깝게 보존하기 위한 raw memo다.
아직 요구사항 확정 문서가 아니며, 이후 코드 확인과 대화를 거쳐 `requirements.md`, `design.md`, `tasks.md`로 정제한다.

## 원문 메모

todo screen 구성 추가 (ios)

1. header

왼쪽에

TODAY (큰 글자) March 3 (작은글자 회색) <- ios 앱스토어 참고한 디자인.

(한글: 오늘 5월 3일 )

오른쪽

... 버튼

메뉴클릭하면 시간순, 카테고리순 선택가능

(현재는 기본적으로 시간순) 다른메뉴들도 추가 예정.

+ 완료된 항목 가리기 / 완료된 항목 보기 추가하자. <- 이거는 리스트 배치??라고 해야되나? 이런게 바뀌는거니까 지금 작업하는게 나을지도??

그리고 헤더에서 메뉴 클릭시 어떤식으로 작동되게 할지 고민이네, ios는 일단 context 메뉴 사용하잖아?

이게 헤더쪽을 네이티브 쓸지 어쩔지 그거에 따라서 context 메뉴로 할지 아니면 우리 일정 아이템 클릭시 나오는 메뉴 이거 공통 컴포넌트로 만들었던 기억이 있는데

이걸로 써야될지 고민이네, 안드로이드도 생각해야돼서... 안드로이드는 일단 저런거 클릭하면 아래에서 위로 올라오는 액션 메뉴들 아님??

앱개발이 첨이라 잘모르것네.. 이건 대화해보자.

그리고 헤더는 기본 상태는 일반 페이지 취급? 이런식으로 식으로 딱 들어와서는 제목이랑 오른쪽에 메뉴밖에 없지만, 스크롤을 이동시키면

일반 header 처럼 맨위에 bar 생기고 타이틀 가운데 있는형태 (ios 기본형태)

근데 너가 한번 검토해줘.

title 있는 줄에 같이 ... 버튼을 두는게 나은지, 아니면 맨위에 bar 를 두고 맨 오른쪽에 ...버튼 그다음줄에 title이 나오는게 좋은지도. 이게 아래에

후술할 선택모드일때도 고려해야되어서.

아 근데 이제 보니까 그냥 맨위에 bar를 두고 맨 오른쪽에 ... 를 하는게 낫겠네.. 이게 선택모드 일때는 또 header에 < 뒤로 버튼이 나와야 해서 선택모드 진입시

갑자기 title bar가 생기는게 이상할수도...

만약 이렇게 가면 1. haeder 다음 2번째 영역은 title 영역이 되어야함.

header는 ios 앱들 처럼 스크롤 내리면 맨위에 고정되는 형태인건 알지?

2. 캘린더 위젯

캘린더 위젯은 weekly <-> monthly 2가지 모드가 있어서 높이가 달라질수있음.

3. 즐겨찾기

4. 캘린더 위젯에서 selected 된 날짜의 일정 리스트

## 추가 사항

- ios 선택모드 추가.

아이템을 꾹 누르면 나오는 메뉴에서 '선택' 추가

1번에서 ...버튼 누르면 '일정 선택' 추가.

선택모드로 진입.

선택모드에도 일정 아이템 나오지만, 일정 아이템 컴포넌트에서 선택모드를 추가하자.

왼쪽에는 기존 일정 완료 버튼 대신 ios 체크박스 표시.

그리고 체크박스 클릭시에 selected 상태도 디자인에 추가해야됨.

선택모드에서는

1. 헤더에서 타이틀은

일정 선택으로 바뀌고, 선택시엔 선택된 항목 1개 이런식으로

이런 디자인은 지금 ios앱 미리 알림을 보면서 적고있음.

아 그리고 방금 봣는데 이거 전환시에 list item의 일정완료는 스르륵 사라지고 선택 체크박스는 왼쪽에서 오른쪽에서 스르륵 나오게 하는게 낫겠네.

그리고 다시 선택모드 해제시에는 체크박스는 오른쪽에서 왼쪽으로 스르륵 사라지고 일정완료 버튼이 나오게 해야될듯.

그리고 선택 모드 전환시에 헤더에 < 뒤로 버튼 왼쪽에 생김. 그리고 지금 미리 알림에서는

header에 < 뒤로 그리고 맨 오른쪽에 완료 라고 나오고 bottom navi가 선택된 항목들로 어떤 작업할지 아이콘으로 나오는데 우리는 어떤식으로 하는게 나으려나..

일단 기능은 삭제, 완료됨으로 표시(완료된 항목은 그냥 냅두고), 즐겨찾기 추가(즐겨찾기 추가되있는 항목은 그냥 냅두고), 이동(카테고리 이동)

이게 우리는 <뒤로 가 있으니까 완료 버튼 대신 ... 버튼을 그대로 써야되나... 근데 우리가 선택모드 전에 ...버튼이 있었잖아. 이거랑 부딪히지 않나... 잘모르겠네.. 검토부탁.

ux적으로 어떤게 나으려나. 이건 대화해보고 freeze하는걸로..

이동(카테고리) 이거는 bottom에서 위로 올라오는 full screen 모달이 되어야 할듯. <- 이건 아마 공통으로 쓰고 있는 카테고리 추가시에 나오는 모달 그거랑

같을듯. 이건 확인 필요함. 어떤방식으로 쓸지. 아마 모달은 ios, 안드로이드 공통으로 하려고 expo? 에서 제공하는거 썻었을걸..

아 모달이 아니라 페이지로 되어있겠구나?? 이거 확인좀. 페이지면 페이지로 하는게 낫지. 아래에서 쭉올라오고 잡아서 아래로 내리면 닫히는 그거 있잖아.

그리고 디자인은 미리 알림보고 쓰고있는건데

여기서는 맨위에 왼쪽 취소, 오른쪽 이동 이렇게 파란 글씨로 버튼있고 <- 이것도 header 공통? ios 네이티브로?? 잘모름. expo에서 제공하는걸로 하는건지..

그리고 아래에

'카테고리'라는 글자

그리고 아래는 group형식의 디자인되어있고

카테고리 목록이 쭉 나열되게. 그리고 선택시 맨 오른쪽에 v 표시.

그리고 삭제 누르면

미리 알림에서 보니까 아래에서

[n개의 미리 알림 삭제] 빨간색

살짝 띄고

[취소] 파란색

이런식으로 올라오네.

<- 이것도 expo에서 네이티브를 쉽게 제공하는거?? 아니면 네이티브?

아, 그리고 이것도 추가. 우리 일정 삭제 시에 바로 삭제 되나?? 아니면 물어보고 삭제하나?

물어보고 삭제하는거면 그냥 바로 삭제하게. 왜냐면 미리알림에서도 이런식으로 하고 있네.

그리고 카테고리 삭제시에도 어떤식으로 나오고 있지?

미리알림에서 보면 여기서는 가운데 dialog 나오는 형태로? 하는게 나을듯?

큰글씨 '카테고리 이름' 카테고리를 삭제하겠습니까?

아래 작은글씨, 이 카테고리의 모든 일정이 삭제됩니다.

아래 버튼 두개 취소(파란색) 삭제(빨간색)

다시 선택모드로 돌아오면

우리가 시간순, 카테고리순으로 나오고 있잖아.

선택모드로 들어가면 리스트는 바뀌는게 아니라 그상태 그대로에서 작동되게 할거임.

## 별도 사항

이거는 또 다른 사항인데

이건 언제 작업해야되는지 모르겠는데

일정 추가시에 날짜를 정하잖아? 근데 날짜 지정안함. 이것도 옵션으로 넣을예정임.

날짜 지정안한거는 언젠가는 해야할일로 사람들이 사용하게 할거고 이거는 기본적으로 항상 노출됨.

## 메모 상태

일단 생각나는대로 쭉 적은 상태.

이걸 새 문서로 저장할지, 기존 문서에 추가할지, 바로 하나씩 해가면서 정리할지, 정리해서 넣어야 하는지 아직 결정 전.
