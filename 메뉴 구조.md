# ListItem

## ListItem 종류와 디자인
- 일정 List Item (Plain List)
- 카테고리 List Item (Inset Grouped List)

## 공통 규칙
- ios, 안드로이드 각각 패딩 / 마진 / 폰트크기 등등.. 네이티브 디자인을 사용한다. (system 값을 따르게)
- 현재 있는 CategoryManager 엔진을 기준으로 한다.
- 일정 List Item 은 화면마다 따로 새로 만들지 않고, 현재 만들어둔 reorder / menu / swipe 네이티브 엔진 기준으로 재사용한다.
- 화면마다 달라지는 것은 일정 List Item 자체가 아니라, 섹션 구조 / 정렬 방식 / reorder 가능 범위 / drop 규칙 / 헤더 방식이다.

## 공통 기능
- ios
    - 꾹 누르면 커스텀 네이티브 메뉴 나옴 (Menu 구성은 각 screen 마다 다름)
    - 그상태에서 움직이면 순서변경 가능
    - ListItem 스와이프 하면 메뉴 나옴
- android
    - 꾹 누르면 순서변경 가능
    - ⋮ 아이콘 클릭시 하단에서 액션 메뉴가 올라옴 (Menu 구성은 각 screen 마다 다름)
    - ListItem 스와이프 하면 메뉴 나옴

## iOS reorder 엔진 선택 규칙
- 단일 flat list reorder 화면은 UIKit built-in reorder를 우선 사용한다.
    - 예: My Page 카테고리 목록, TODO SCREEN `시간순`의 시간 없는 일정 영역, CATEGORY SCREEN, FAVORITE SCREEN
    - 이유: source row가 리스트 흐름에서 빠지며 주변 row가 자연스럽게 압축되고, 시스템 reorder 안정성/접근성을 활용할 수 있다.
- 카테고리 그룹 간 이동이 필요한 화면은 custom drag engine을 사용한다.
    - 예: TODO SCREEN `카테고리순`, ALL TODOS SCREEN
    - 이유: cross-category move, 접힌 카테고리 hover auto-expand, 같은 gesture 유지, category header reorder, Inbox pinned drop constraint가 필요하다.
- custom drag engine 화면에서는 일정 / 카테고리 drag 중 drop 위치를 list 전체 폭 indicator line으로 표시한다.
- 따라서 `NativeManagedList` facade와 저장 contract는 공유하지만, iOS 내부 reorder 엔진은 화면 구조에 따라 다를 수 있다.

## 디자인

### 앱 색상 테마 정책
- 앱 색상은 iOS / Android 시스템 색상을 기본값으로 따로 쓰지 않고, Material 3 color role 기반의 공통 테마 토큰을 사용한다.
- iOS native header는 navigation chrome으로 보고 UIKit / native-stack 기본 appearance를 우선 따른다.
    - iOS native header의 background, tintColor, title color는 기본적으로 Material token을 강제 주입하지 않는다.
    - iOS header shadow / hairline 같은 최소 옵션만 화면 UX에 맞게 제어한다.
    - iOS 26 modal header는 시스템 scroll-edge / Liquid Glass 계열의 상단 gradient opacity 처리를 정상 동작으로 본다.
    - `CategoryColorScreen` 테스트 기준, scroll 가능한 색상 선택 modal에서 native header가 콘텐츠 위에 gradient opacity로 겹쳐지는 동작을 확인했다.
    - 이유: iOS scroll-edge appearance, Liquid Glass, 라이트 / 다크 / 접근성 처리는 시스템 기본 동작에 맡기는 것이 안정적이다.
- Material 3 앱 공통 테마 토큰은 content 영역부터 적용한다.
    - 예: 화면 배경, list / card / row, content text, custom action, native list row, preview / menu / custom interaction surface
- Android native / custom header는 Material 3 앱 공통 테마 토큰을 적용할 수 있다.
- iOS native list, Android native UI는 플랫폼별 동작과 레이아웃은 따르되, content 색상은 앱 공통 테마 토큰을 주입해서 맞춘다.
- 테마는 실시간으로 매 렌더마다 생성하지 않는다.
    - 사용자는 `default`, `dark`, `orange` 같은 정해진 테마 preset 중 하나를 선택한다.
    - 사용자 설정에는 `selectedThemeId`만 저장한다.
    - 앱은 `selectedThemeId`에 해당하는 미리 정의된 theme object를 읽어서 사용한다.
- Material 3 palette 생성은 개발 시점 또는 테마 preset 생성 시점에만 수행한다.
    - 필요 시 `@material/material-color-utilities`를 사용해 seed color에서 `primary`, `surface`, `surfaceContainer`, `onSurface`, `outline`, `error` 같은 role을 생성한다.
    - 런타임에서는 생성된 token 값을 참조만 한다.
- `react-native-paper` 같은 Material 컴포넌트 라이브러리는 기본 도입하지 않는다.
    - 이유: 현재 앱은 `NativeManagedList`, native header, custom native interaction을 기준으로 가고 있으므로 컴포넌트 라이브러리까지 섞지 않는다.
    - Material은 색상 체계 / role / shape / motion 참고 기준으로 사용한다.
- native module에는 필요한 색상 role만 props / config로 전달한다.
    - 예: `background`, `surface`, `surfaceContainerLow`, `surfaceContainer`, `onSurface`, `onSurfaceVariant`, `primary`, `outline`, `error`

### 선택 modal 표준
- 단순 선택 화면은 Expo Router `modal` + native Stack header + `NativeSelectionList` 조합을 기본으로 한다.
    - 예: 카테고리 색상 선택, 일정 이동 대상 카테고리 선택, 단순 설정값 선택
- iOS에서는 header background / tint / title color를 강제하지 않고 native/system appearance를 따른다.
    - iOS 26의 상단 gradient opacity / Liquid Glass 계열 동작은 정상 동작으로 본다.
- Android에서는 modal 화면 header / content 색상에 앱 공통 Material 3 token을 적용할 수 있다.
- modal header 액션은 화면의 task에 맞춘다.
    - 색상 선택: 오른쪽 `완료`
    - 일정 이동 대상 카테고리 선택: 왼쪽 `취소`, 오른쪽 `이동`
- 선택값은 row tap 시 pending 상태로 반영하고, 오른쪽 header action을 눌러 최종 적용한다.
- 일정 이동 대상 카테고리 선택 modal은 `todo/category-select` route를 사용한다.
    - 진입: 일정 메뉴의 `이동`, 선택모드 하단 action bar의 `이동`
    - 파라미터: 단일 이동은 `todoId`, bulk 이동은 comma-separated `todoIds`
    - 목록: 현재 카테고리 목록을 `NativeSelectionList`로 표시
    - 현재 카테고리는 checkmark / selected 상태로 표시
    - 선택한 대상 카테고리가 현재 카테고리와 같으면 `이동`은 비활성화한다.
    - `이동` 실행 시 `categoryId`를 변경하고 대상 카테고리의 맨 아래 `category_order`로 append 한다.
    - bulk 이동 시 선택된 일정들을 target category 마지막 `category_order` 뒤에 순차 append 한다.
    - 기존 `custom_order`, `favorite_order`, 날짜 / 시간 필드는 보존한다.
    - 적용 후 modal을 닫고 원래 화면으로 복귀한다.
    - iOS 시뮬레이터에서 `김국진` 선택 후 `이동`을 Maestro + SQLite로 검증했다.
- 검색이 필요한 긴 선택 화면은 이 표준에서 분리한다.
    - 예: 타임존 선택, 언어 선택

### 일정 완료 / 완료취소 토글 버튼
- ios + android 공통으로 쓸 수 있는 커스텀 토글 버튼 디자인을 사용한다.

### Native List 스타일 분류
- `group.category`
    - 사용 위치: `My Page > 카테고리 목록`
    - 대상: `category`
    - 스타일 상태: `normal`, `menuPreview`, `dragPreview`
    - iOS Inset Grouped List처럼 여러 row가 한 카드 묶음으로 보이는 스타일이다.
    - `Inbox`는 pinned / non-reorderable modifier를 가진다.
    - `카테고리 추가` row는 action row이며 reorder / menu 대상이 아니다.

- `list.todo`
    - 사용 위치: `TODO SCREEN`, `CATEGORY SCREEN`, `FAVORITE SCREEN`, `ALL TODOS SCREEN`
    - 대상: `todo`
    - 스타일 상태: `normal`, `menuPreview`, `dragPreview`
    - 화면마다 섹션 구조 / 정렬 방식 / reorder 가능 범위 / drop 규칙은 달라질 수 있지만, 일정 row의 기본 스타일 체계는 공유한다.

- `list.header`
    - 사용 위치: `TODO SCREEN > 카테고리순`, `ALL TODOS SCREEN`, 상단 즐겨찾기 그룹
    - 대상: `sectionHeader`
    - 스타일 상태: `normal`, `menuPreview`, `dragPreview`
    - 카테고리 header와 즐겨찾기 header는 기본 스타일은 공유하되, behavior modifier는 분리한다.
    - 카테고리 header는 reorder 가능할 수 있고, 즐겨찾기 header는 pinned / non-reorderable 이다.
    - 오른쪽에는 현재 화면 조건에서 해당 그룹에 표시되는 일정 개수와 접기 / 펼치기 아이콘을 표시한다.

- `list.divider`
    - 사용 위치: 상단 즐겨찾기 그룹과 아래 일반 목록 사이
    - 대상: `sectionDivider`
    - 스타일 상태: `normal` only
    - menu / drag / swipe 대상이 아니다.

### Native List 상태 modifier
- `completed`: 완료된 일정 label dim 처리
- `disabled`: 비활성 row alpha / interaction 제한
- `pinned`: Inbox, 즐겨찾기 header처럼 위치 고정
- `collapsed`: 접힌 그룹 header 상태
- `hidden`: 접힌 그룹 안에서 화면에 보이지 않는 row 상태
- `selected`: 선택모드에서 선택된 일정 row 배경 / selection control 상태에 사용

### 일정 선택모드
- 진입:
    - 헤더 action의 `선택`
    - 일정 long-press menu의 `선택`
- 화면 이동 없이 현재 list가 선택모드로 전환된다.
- header:
    - 왼쪽은 원래 페이지의 back/cancel button을 유지한다.
    - 중앙 title은 `일정 선택` 또는 `n개 선택됨`으로 바뀐다.
    - 오른쪽은 `완료`로 바뀌고, 누르면 선택모드를 종료한다.
- body:
    - row tap은 선택 toggle로 동작한다.
    - 기존 완료 toggle 자리는 selection checkbox / multiselect control로 대체한다.
    - 선택된 row는 `selected` 상태로 표시한다.
    - swipe / context menu / reorder / collapse는 비활성화한다.
- bottom:
    - bottom tab bar를 숨기고 선택 항목 action bar를 표시한다.
    - 기본 action은 `삭제`, `완료`, `즐겨찾기`, `이동`이다.
    - 0개 선택 시 action은 disabled 상태이다.
    - `이동`은 공통 `todo/category-select` modal로 연결한다.
- 적용 범위:
    - `FAVORITE SCREEN`, `ALL TODOS SCREEN`, `CATEGORY SCREEN`부터 적용한다.
    - `TODO SCREEN`은 calendar / title / 정렬모드와 함께 별도 UX 검증 후 적용한다.

### Native List preview / reorder 제약
- custom drag engine을 사용하는 화면의 `menuPreview`, `dragPreview`는 앱에서 직접 디자인한다.
- UIKit built-in reorder를 사용하는 flat list 화면은 시스템 reorder preview 제약을 따른다.
- swipe action 디자인은 `normal / menuPreview / dragPreview`와 별도 축으로 둔다.
- iOS 구현은 `group.category`, `list.todo`, `list.header`별 preview corner resolver를 분리한다.
- iOS preview shadow / scale / translation 값은 phase 공통 factory에서 관리한다.
- `group.category`의 실제 reorder drag preview는 UIKit built-in reorder 제약을 따르며, custom overlay preview는 메뉴 진입 상태에만 적용한다.
- menu preview 에서 reorder 로 전환할 때는 가능하면 기존 preview snapshot 을 이어서 사용하고, source cell 을 먼저 복구하지 않는다.
- preview 시각 디자인 polish 는 명시 요청 전까지 보류한다.

### 일정 List Item
    - ios
        - 왼쪽: 일정 완료 / 완료취소 토글 버튼 + 라벨 (완료된 일정은 label dim 처리)
        - 오른쪽: 없음 / 즐겨찾기 추가 / 해제 토글 버튼 별 filled / outline 아이콘 (즐겨찾기 스크린에서만 사용)
    - android
        - 왼쪽: 일정 완료 / 완료취소 토글 버튼 + 라벨 (완료된 일정은 label dim 처리)
        - 오른쪽: 즐겨찾기 추가 / 해제 토글 버튼 별 filled / outline 아이콘 (즐겨찾기 스크린에서만 사용) + ⋮ 아이콘

### 카테고리 List Item
    - ios
        - 왼쪽: 카테고리 색상 아이콘 / 라벨
        - 오른쪽: 카테고리 안에 있는 일정의 갯수 + 오른쪽 arrow icon
    - android
        - 왼쪽: 카테고리 색상 아이콘 / 라벨
        - 오른쪽: ⋮ 아이콘

### 일정 List Item Sub Label 표시
 - ios + android
    - Star 아이콘 + 즐겨찾기 추가 된 일정: 즐겨찾기 표시 // 예외: 즐겨찾기 스크린에서는 표시 안함.
    - Calendar Today 아이콘 + 일반일정: 일정의 마감일 / 기간이 있는 일정: 일정의 기간 (표시: 시작일 - 마감일) -> 마감일이 지난 일정은 Warning 아이콘으로 바뀌고, 기간이 지난 일정이라고 표시한다. (표시: 시작일 - 마감일 (기간이 지난 일정))
    - Alarm 아이콘 + 시간이 있는 일정: 일정의 시간 (표시: 시작시간 - 종료시간)
    - Cached 아이콘 + 반복 일정: 반복 설정 텍스트를 표시한다. (예: 매주 금요일, 매달 15일, 매일) // 예외: TODO SCREEN 상단 즐겨찾기 그룹에서는 다음 일정 표시 가능

### 카테고리 List Item Sub Label 표시
 - ios
    - 표시안함
- android
    - 표시함 (n개의 일정)

## listItem 메뉴 구성

### 일정 List Item
- 일정 (보기), 수정, 이동, 즐겨찾기 추가 / 해제, 일정 복사, 일정 삭제
- `이동`은 현재 화면에서 drag target이 보이지 않는 경우에도 공통 `카테고리 선택` modal을 연다.
- 오른쪽에서 왼쪽으로 스와이프 - 삭제
- 왼쪽에서 오른쪽으로 스와이프 - 즐겨찾기 추가 / 해제

### 카테고리 List Item
- 수정, 삭제
- 오른쪽에서 왼쪽으로 스와이프 - 삭제


## 일정 listItem 반복일정 완료 및 표시 규칙 (일정을 기간에 관계없이 항상 보여줘야 되어야 하는 스크린에서 사용 (TODO SCREEN - FAVORITE 부분, FAVORITE SCREEN, CATEGORY SCREEN))
- 완료 규칙
    - 오늘 날짜를 기준으로 오늘을 포함한 가까운 날짜를 우선적으로 완료 처리한다.
    - 완료 처리하면 사라지지 않고, label dim 처리한다.

- 표시 규칙
    - 오늘 날짜를 기준으로 오늘을 포함한 가까운 날짜를 우선적으로 표시
    - 다음 일정은 표시하지 않는다. 매일 12시 마다 오늘 날짜를 기준으로 반복 일정을 계산해서 표시 해준다. 다음 반복일정은 다음날 표시된다. 그리고 만약 매주 목요일 반복 일정이 있었을떄, 오늘이 화요일일때, 완료 처리하면 다음주 목요일이 지나고 금요일이 되어야 다음 일정이 표시된다.
일반, 기간이 있는 일정 이런 것은 완료 처리가 쉽지만, 반복 일정의 경우에는 오늘 날짜를 기준으로 오늘을 포함한 가까운 날짜를 우선적으로 완료 시키고 다른 일정과 마찬가지고 label dim 처리한다. 그리고 다음 일정은 표시하지 않는다. 매일 12시 마다 오늘 날짜를 기준으로 반복 일정을 계산해서 표시 되니까 만약 오늘을 포함한 매일 반복일정이 있었고, 오늘 카테고리에 속한 일정 페이지에서 완료 처리했을때, 다음 반복일정은 다음날 표시된다. 그리고 만약 매주 목요일 반복 일정이 있었을떄, 오늘이 화요일일때, 완료 처리하면 다음주 목요일이 지나고 금요일이 되어야 다음 일정이 표시된다.



## CATEGORY (MY PAGE)
- group title 있음 (카테고리)
- my page 화면 안에 있음.
- 카테고리 listItem 사용

## CATEGORY SCREEN
- 카테고리에 속한 일정들이 들어가는 screen
- my page에서 카테고리 listItem 클릭하면 해당 screen으로 이동한다.
- 해당 카테고리에 속한 일정들이 이 screen에 표시된다.
- 일정 listItem 사용

### 일정 완료 시 규칙
- 일정 완료 버튼으로 완료/미완료 토글 가능
- 완료 처리하면 사라지지 않고, label dim 처리한다.

### 순서
- 카테고리 내부 일정 순서를 사용한다.
- 사용자가 순서를 변경 하면 카테고리 내부 일정 순서가 반영되도록 저장되어야 한다.

### reorder
- 사용

### 다른 카테고리 이동
- 여기서는 다른 카테고리가 화면에 보이지 않기 때문에 drag 해서 다른 카테고리로 이동시키지 않는다.
- 메뉴의 `이동`으로만 다른 카테고리로 이동 가능하다.
- 메뉴의 `이동`은 카테고리 변경과 함께 대상 카테고리 맨 아래 append 로 처리한다.
- 정확한 위치 조정은 reorder 가능한 화면에서 처리한다.

## TODO SCREEN
오늘 또는 selected된 날짜에 속한 일정들이 들어가는 스크린

### 일정 완료 시 규칙
- 일정 완료 버튼으로 완료/미완료 토글 가능
- 완료된 일정은 사라짐 (즐겨찾기는 예외. 아래 참조)

### 일정 탭 동작
- 일정 클릭 시 아래에서 일정 상세정보가 bottom sheet 형태로 올라온다.

### 헤더 메뉴
- 헤더 부분 오른쪽 ⋮ 아이콘을 누르면 정렬을 변경할 수 있다.
- 마지막으로 선택한 정렬 모드(`시간순 / 카테고리순`)는 로컬에 저장하고, 다음 진입 시 그대로 복원한다.

### 순서
- 시간순, 카테고리순
- 즐겨찾기는 무조건 맨위 고정.
    - 즐겨찾기 그룹은 비어 있어도 header를 항상 표시한다.
    - 즐겨찾기 그룹은 카테고리 그룹처럼 접기 / 펼치기가 가능해야 한다.
    - 즐겨찾기 그룹의 접힘 / 펼침 상태는 TODO SCREEN 안에서 별도로 저장한다.
    - 즐겨찾기 그룹과 아래 일반 목록 사이에는 iOS grouped list처럼 회색 divider 공간을 둔다.
    - 즐겨찾기에 등록된 일정은 날짜에 상관없이 항상 보여준다.
    - 반복 일정의 경우, 카테고리안의 일정에서 정해놓은 규칙에 따라 다음 반복일정을 계산해서 보여준다. (카테고리안의 일정 규칙: 반복 일정이라면, 오늘 날짜를 기준으로 오늘을 포함한 가까운 날짜를 우선적으로 보여준다. 다음 일정은 표시하지 않는다. 매일 12시 마다 오늘 날짜를 기준으로 반복 일정을 계산해서 표시 해준다. 다음 반복일정은 다음날 표시된다. 그리고 만약 매주 목요일 반복 일정이 있었을떄, 오늘이 화요일일때, 완료 처리하면 다음주 목요일이 지나고 금요일이 되어야 다음 일정이 표시된다.)
    - TODO SCREEN 상단 즐겨찾기 그룹의 반복 일정 sub label 은 반복 설정 텍스트 대신 `다음 일정` 기준으로 표시 가능하다. (예: 다음 일정 5월 3일, 다음 일정 내일)

    - 즐겨찾기에 등록된 일정은 아래 목록들에서는 안보여준다.
    - 즐겨찾기에 등록된 일정은 고정되어 계속 해서 추적하는 일정이기 때문에 완료 처리 시 목록에서 사라지지 않고, label dim 처리한다. (아래 추가사항의 중요 2 항목 참조)
    - 일반 일정을 상단 즐겨찾기 그룹으로 drag & drop 하면 즐겨찾기에 추가된다.
    - 즐겨찾기 그룹으로 drop 할 때는 `favorite_order`만 변경하고, 기존 `categoryId`, `custom_order`, `category_order`는 유지한다.
    - 상단 즐겨찾기 그룹에서 아래 일반 목록으로 끌어내리면 즐겨찾기를 해제한다.
    - 시간순에서 즐겨찾기 일정을 시간 없는 일반 일정 사이로 끌어내리면 `favorite_order`를 비우고 해당 `custom_order` 위치를 반영한다.
    - 시간순에서 즐겨찾기 일정을 시간 지정 일정 영역으로 끌어내리면 `favorite_order`를 비우고 시간 없는 일반 일정 영역 맨 위 `custom_order`로 처리한다.
    - 카테고리순에서 즐겨찾기 일정을 일반 카테고리로 끌어내리면 `favorite_order`를 비우고 대상 `categoryId` / `category_order` 위치를 반영한다.
    - 접힌 즐겨찾기 그룹으로 drop 성공 시 즐겨찾기 그룹을 펼침 상태로 저장한다.
    - 즐겨찾기 그룹이 비어 있고 접혀 있으면 hover 중 자동 펼침은 필수로 하지 않지만, drop은 허용한다.

1. 시간순
    - 시작일 기준 일정의 시간 순서
    - 시작일이 같다면, 시간이 있는 일정은 시간 순서로 먼저 표시한다.
    - 시간이 있는 일정은 사용자 reorder 불가
    - 시간이 있는 일정을 drag 하거나, 다른 일정을 시간이 있는 일정 영역에 drop 하려고 하면 drop 을 거부하고 원래 위치로 복귀시킨다.
    - 예외: 시간이 있는 일정도 상단 즐겨찾기 그룹으로는 drag & drop 가능하다. 이 경우 시간순 위치는 바꾸지 않고 `favorite_order`만 저장한다.
    - 시간이 없는 일정은 시간이 있는 일정 아래에 표시한다.
    - 시간이 없는 일정끼리는 사용자 reorder 허용
    - 시간이 없는 일정의 순서는 todo 원본의 `custom order`로 저장한다.
    - 시간이 없는 일정에 시간이 설정되면 시간순 영역으로 이동하며, 기존 `custom order` 값은 보존하지만 정렬에는 사용하지 않는다.
    - 반복 일정 / 기간 일정도 selected된 날짜에 표시되는 항목에 시간이 없으면 시간 없는 일정 영역에서 reorder 가능하다.
    - 새 todo 생성 시 시간이 없으면 시간 없는 일정 영역의 맨 아래 `custom order`로 추가된다.
    - 1. 년 반복 일정
    - 2. 기간 일정
    - 3. 오늘 일정 (하루종일)
    - 4. 월 반복 일정
    - 5. 주간 반복일정
    - 6. 매일 반복 일정
    - 7. 시간이 있는 일정 & 기간과 시간이 있는 일정 (시간순 ⬇️)

2. 카테고리순
    - 카테고리의 순서와 카테고리 안에 있는 일정의 순서대로 표시 (selected된 날짜 기준)
    - 이 모드는 `ALL TODOS SCREEN`과 같은 category-grouped interaction model을 재사용하되, selected된 날짜에 보이는 일정만 각 카테고리 아래에 표시한다.
    - 카테고리순으로 볼 때는 오늘 일정이 없어도 모든 카테고리 group title 을 표시한다.
    - 카테고리 group header 오른쪽 아이콘을 누르면 해당 카테고리 그룹이 접히거나 펼쳐진다.
    - 카테고리 group header 오른쪽에는 selected된 날짜 기준으로 해당 그룹에 표시되는 일반 일정 개수를 표시한다.
    - 카테고리순으로 볼 때는 일정 reorder 허용
    - 카테고리순으로 볼 때는 꾹 눌러서 다른 카테고리로도 이동 가능. (이동한 일정은 카테고리 일정 페이지에서 순서가 반영됨)
    - 다른 카테고리로 이동할 때도 대상 카테고리 안의 일정 사이사이로 드롭 가능해야 한다.
    - 다른 카테고리로 이동하면 대상 카테고리의 보이는 위치 기준으로 삽입된다.
    - ios 에서는 카테고리 group header 를 꾹 누르면 커스텀 메뉴가 열리고, 메뉴가 열려 있는 동안 해당 카테고리는 임시로 접힌 상태를 유지한다.
    - ios 에서 메뉴가 열린 상태로 header를 끌어 reorder를 시작하면, 이동 중에는 펼쳐져 있던 전체 카테고리 group이 임시로 접힌다.
    - ios 에서 메뉴만 닫으면 임시 접힘 상태는 원래 상태로 복구된다.
    - android 에서는 카테고리 group header 를 꾹 누르면 카테고리 reorder 가능
    - 카테고리 reorder 시에는 카테고리 순서만 바뀌고, 카테고리 안의 일정 순서는 유지된다.
    - Inbox 같은 system category는 최상단 고정이며, 다른 카테고리를 그 위로 이동할 수 없다.
    - 접힌 카테고리에 drag hover 가 일정 시간(약 0.5초 전후) 유지되면 해당 카테고리가 자동으로 펼쳐진다.
    - 자동으로 펼쳐진 뒤에는 해당 카테고리 내부의 보이는 gap 위치에 그대로 drop 가능해야 한다.
    - 접힌 카테고리로 drop 성공 시 해당 카테고리를 펼침 상태로 저장한다.
    - 카테고리 안에 일정이 없으면 hover 중 자동 펼침은 필수로 하지 않지만, drop은 허용한다.
    - 카테고리순으로 볼 때는 즐겨찾기 그룹으로 드롭 가능
    - 즐겨찾기 그룹으로 드롭한 일정은 해당 카테고리에서 숨겨지고, 원래 카테고리/카테고리 내부 순서는 유지한다.
    - 즐겨찾기 해제 시에는 원래 숨겨놨던 일반 일정 목록이 다시 보이게 한다.

### 추가 사항
- 해당 스크린의 일정 목록은 기본적으로 Plain List이지만, Inset Grouped List 처럼 그룹명이 들어간다. 즐겨찾기 / 기본일정들은 selected된 날짜로 (오늘, 내일, 모레, 특정 날짜 등) 그룹명이 표시된다.
- 즐겨찾기 추가 된 일정 목록은 항상 맨위에 고정.
- 중요 1. 일정 순서 변경에서 일정을 끌어서 이동 시킬 때, 즐겨찾기 안으로도 넣을수 있음. (이동한 일정은 즐겨찾기 목록에 추가됨), 그리고 즐겨찾기로 추가 시킬때, 순서도 사용자가 옮긴 순서가 반영됨.
- 중요 2. 즐겨찾기에 등록된 일정은 고정되어 계속 해서 보여주는 일정이기 때문에 완료 처리 시 목록에서 사라지지 않고, label dim 처리한다. (이건 즐겨찾기 스크린에서도 동일하게 적용되어야함) 만약 반복 일정이라면, 앞서 카테고리안의 일정에서 정해놓은 규칙대로 오늘 날짜를 기준으로 오늘을 포함한 가까운 날짜를 우선적으로 완료 시키고 다른 일정과 마찬가지고 label dim 처리한다. 그리고 다음 일정은 표시하지 않는다. 매일 12시 마다 오늘 날짜를 기준으로 반복 일정을 계산해서 표시 되니까 만약 오늘을 포함한 매일 반복일정이 있었고, 오늘 카테고리에 속한 일정 페이지에서 완료 처리했을때, 다음 반복일정은 다음날 표시된다. 그리고 만약 매주 목요일 반복 일정이 있었을떄, 오늘이 화요일일때, 완료 처리하면 다음주 목요일이 지나고 금요일이 되어야 다음 일정이 표시된다.


## ALL TODOS SCREEN
날짜 제한 없이 전체 일정이 들어가는 스크린

### 목적
- 전체 일정을 카테고리 기준으로 한눈에 보고 관리하는 화면
- TODO SCREEN 처럼 selected된 날짜 기준으로 보여주는 화면이 아니라, 날짜와 관계없이 전체 일정을 보여주는 화면
- 카테고리 / 일정의 순서를 구조적으로 관리하는 화면

### 리스트 구조
- 상단에는 즐겨찾기 그룹을 고정 표시한다.
- 즐겨찾기 그룹은 비어 있어도 header를 항상 표시한다.
- 즐겨찾기 그룹은 카테고리 그룹처럼 접기 / 펼치기가 가능해야 한다.
- 즐겨찾기 그룹의 접힘 / 펼침 상태는 ALL TODOS SCREEN 안에서 별도로 저장한다.
- 즐겨찾기 그룹과 아래 카테고리 목록 사이에는 iOS grouped list처럼 회색 divider 공간을 둔다.
- 즐겨찾기 그룹은 `TODO SCREEN > 카테고리순`과 같은 top favorites interaction model을 재사용한다.
- 즐겨찾기에 등록된 일정은 아래 카테고리 목록에서는 중복 표시하지 않는다.
- 카테고리별 그룹으로 일정이 묶여서 표시된다.
- `TODO SCREEN > 카테고리순`과 같은 category-grouped interaction model을 재사용하되, 이 화면은 selected된 날짜 필터 없이 전체 일정을 넣는다.
- 각 카테고리 그룹 아래에 해당 카테고리에 속한 일정 listItem 들이 표시된다.
- 카테고리 그룹 title은 카테고리 listItem 스타일을 기준으로 사용한다.
- 일정은 일정 listItem 스타일을 사용한다.
- 반복 일정도 원본 일정 1개만 표시한다. occurrence를 날짜별로 펼쳐서 여러 줄로 표시하지 않는다.
- 반복 일정의 sub label은 가까운 날짜 계산이 아니라 반복 설정 텍스트를 표시한다. (예: 매주 금요일, 매달 15일, 매일)

### 카테고리 그룹 헤더
- 왼쪽: 카테고리 색상 아이콘 / 카테고리명
- 오른쪽: 일정 개수 + 위 / 아래 아이콘
- 오른쪽 아이콘을 누르면 해당 카테고리 그룹이 접히거나 펼쳐진다.
- 카테고리 그룹이 접히면 해당 카테고리에 속한 일정 listItem 들은 보이지 않는다.

### 카테고리 그룹 헤더 인터랙션
- ios
    - 카테고리 그룹 헤더를 꾹 누르면 커스텀 메뉴가 열리고, 메뉴가 열려 있는 동안 해당 카테고리는 임시로 접힌 상태를 유지한다.
    - 커스텀 메뉴에는 `카테고리로 이동` 항목이 있으며, 선택 시 해당 카테고리 상세 일정 화면으로 이동한다.
    - Inbox는 `카테고리로 이동`은 가능하지만 이름 변경 / 삭제는 불가하다.
    - 메뉴가 열린 상태로 header를 끌어 reorder를 시작하면, 이동 중에는 펼쳐져 있던 전체 카테고리 group이 임시로 접힌다.
    - 메뉴만 닫으면 임시 접힘 상태는 원래 상태로 복구된다.
- android
    - 카테고리 그룹 헤더를 꾹 누르면 카테고리 reorder 가능
- 카테고리 reorder 시에는 카테고리 순서만 바뀌고, 카테고리 안의 일정 순서는 유지된다.
- Inbox 같은 system category는 최상단 고정이며, 다른 카테고리를 그 위로 이동할 수 없다.

### 일정 listItem 인터랙션
- ios
    - 꾹 누르면 커스텀 네이티브 메뉴가 나오고, 그 상태에서 움직이면 reorder 가능
- android
    - 꾹 누르면 reorder 가능
    - ⋮ 아이콘 클릭시 하단에서 액션 메뉴가 올라옴
- 일정 listItem 는 같은 카테고리 안에서 순서 변경 가능
- 일정 listItem 를 다른 카테고리 그룹으로 이동 가능
- 다른 카테고리 그룹으로 이동하면 categoryId 와 순서가 같이 변경된다.
- 다른 카테고리 그룹으로 이동할 때도 대상 카테고리 안의 일정 사이사이로 드롭 가능해야 한다.
- 다른 카테고리 그룹으로 이동하면 대상 카테고리의 보이는 위치 기준으로 삽입된다.
- 일정 listItem 를 상단 즐겨찾기 그룹으로 이동 가능하다.
- 상단 즐겨찾기 그룹으로 이동하면 `favorite_order`만 변경되고, 기존 `categoryId`, `custom_order`, `category_order`는 유지된다.
- 상단 즐겨찾기 그룹에서 아래 일반 카테고리 목록으로 끌어내리면 즐겨찾기를 해제하고 대상 `categoryId` / `category_order` 위치를 반영한다.

### 일정 listItem 메뉴 구성
- 공통 일정 List Item 메뉴 구성을 사용한다.
- 즐겨찾기 항목은 일정 상태에 따라 `즐겨찾기 추가` 또는 `즐겨찾기 해제`로 표시된다.
- 오른쪽에서 왼쪽으로 스와이프 - 삭제
- 왼쪽에서 오른쪽으로 스와이프 - 즐겨찾기 추가 / 해제

### 접힘 / 펼침 규칙
- 접힘 상태는 즐겨찾기와 카테고리별로 저장되어야 한다.
- 접힌 즐겨찾기 / 카테고리에도 다른 일정 listItem 를 드롭할 수 있다.
- 일정이 있는 접힌 카테고리나 즐겨찾기에 drag hover 가 일정 시간(약 0.5초 전후) 유지되면 해당 그룹이 자동으로 펼쳐진다.
- 빈 접힌 그룹은 hover 중 자동 펼침은 필수로 하지 않지만, drop은 허용한다.
- 접힌 그룹으로 drop 성공 시 해당 그룹을 펼침 상태로 저장한다.
- 자동으로 펼쳐진 뒤에는 해당 그룹 내부의 보이는 gap 위치에 그대로 drop 가능해야 한다.
- 접힌 그룹 안의 일정은 화면에서 보이지 않지만, 내부 순서 데이터는 그대로 유지된다.

### 순서 규칙
- 카테고리 순서는 My Page 의 카테고리 순서를 기본으로 따른다.
- 카테고리 안 일정의 순서는 CATEGORY SCREEN 에서 사용하는 카테고리 내부 순서를 기본으로 따른다.
- 사용자가 ALL TODOS SCREEN 에서 카테고리 순서를 바꾸면, My Page 카테고리 순서에도 반영된다.
- 사용자가 ALL TODOS SCREEN 에서 일정 순서를 바꾸거나 다른 카테고리로 이동시키면, CATEGORY SCREEN 에도 반영된다.
- 사용자가 ALL TODOS SCREEN 에서 일정을 즐겨찾기 그룹으로 이동시키면, FAVORITE SCREEN 과 TODO SCREEN 상단 즐겨찾기 그룹에도 반영된다.

### 일정 완료 시 규칙
- 일정 완료 버튼으로 완료/미완료 토글 가능
- 이 화면은 전체 보기만 사용한다.
- 완료 처리하면 사라지지 않고, label dim 처리한다.
- 반복 일정은 CATEGORY SCREEN / FAVORITE SCREEN 에서 사용하는 반복 일정 완료 규칙과 동일하게 처리한다.

### 추가 사항
- 이 화면은 TODO SCREEN 의 날짜 중심 화면과 다르므로, selected된 날짜 개념을 사용하지 않는다.
- 이 화면은 전체 구조 관리용 화면이므로, 카테고리 reorder 와 일정 reorder 둘 다 지원한다.
- 즐겨찾기 그룹은 별도 상단 그룹으로 분리한다.
- 즐겨찾기 추가 된 일정은 상단 즐겨찾기 그룹에만 표시하고, 일반 카테고리 목록에서는 숨긴다.


## FAVORITE SCREEN
즐겨찾기 일정들이 들어가는 스크린

### 일정 완료 시 규칙
- 일정 완료 버튼으로 완료/미완료 토글 가능
- 완료된 일정은 label dim 처리
- 일반, 기간이 있는 일정 이런 것은 완료 처리가 쉽지만, 반복 일정의 경우에는 오늘 날짜를 기준으로 오늘을 포함한 가까운 날짜를 우선적으로 완료 시키고 다른 일정과 마찬가지고 label dim 처리한다. 그리고 다음 일정은 표시하지 않는다. 매일 12시 마다 오늘 날짜를 기준으로 반복 일정을 계산해서 표시 되니까 만약 오늘을 포함한 매일 반복일정이 있었고, 오늘 카테고리에 속한 일정 페이지에서 완료 처리했을때, 다음 반복일정은 다음날 표시된다. 그리고 만약 매주 목요일 반복 일정이 있었을떄, 오늘이 화요일일때, 완료 처리하면 다음주 목요일이 지나고 금요일이 되어야 다음 일정이 표시된다.

#### 순서
- 기본은 즐겨찾기 추가된 순서 (오래된 순)
- 사용자가 순서를 변경 하면 favorite 전용 order 가 업데이트 되도록 저장되어야 한다.

#### reorder
- 허용
- favorite 전용 order 를 따로 만들어서 관리하는 방향으로 한다.

#### 일정 listItem 메뉴 구성
- 공통 일정 List Item 메뉴 구성을 사용한다.
- 이 화면에서는 모든 일정이 즐겨찾기 상태이므로 메뉴에는 `즐겨찾기 해제`로 표시된다.
- 오른쪽에서 왼쪽으로 스와이프 - 삭제
- 왼쪽에서 오른쪽으로 스와이프 - 즐겨찾기 해제

#### 추가 사항
- 즐겨찾기 추가/해제 토글 버튼을 클릭하면 즐겨찾기 추가/해제 되면서 별 아이콘 색깔이 변한다.
- 즐겨찾기 스크린에서는 즐겨찾기 해제시 해당 일정이 목록에서 바로 사라진다.
