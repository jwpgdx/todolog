# ListItem

## ListItem 종류와 디자인
- 일정 List Item (Plain List)
- 카테고리 List Item (Inset Grouped List)

## 공통 규칙
- ios, 안드로이드 각각 패딩 / 마진 / 폰트크기 등등.. 네이티브 디자인을 사용한다. (system 값을 따르게)
- 현재 있는 CategoryManager 엔진을 기준으로 한다.
- 일정 List Item 은 화면마다 따로 새로 만들지 않고, 현재 만들어둔 reorder / menu / swipe 네이티브 엔진 기준으로 재사용한다.
- 화면마다 달라지는 것은 일정 List Item 자체가 아니라, 섹션 구조 / 정렬 방식 / reorder 가능 범위 / drop 규칙 / 헤더 방식이다.

## 공통 기능
- ios
    - 꾹 누르면 커스텀 네이티브 메뉴 나옴 (Menu 구성은 각 screen 마다 다름)
    - 그상태에서 움직이면 순서변경 가능
    - ListItem 스와이프 하면 메뉴 나옴
- android
    - 꾹 누르면 순서변경 가능
    - ⋮ 아이콘 클릭시 하단에서 액션 메뉴가 올라옴 (Menu 구성은 각 screen 마다 다름)
    - ListItem 스와이프 하면 메뉴 나옴

## iOS reorder 엔진 선택 규칙
- 단일 flat list reorder 화면은 UIKit built-in reorder를 우선 사용한다.
    - 예: My Page 카테고리 목록, TODO SCREEN `사용자 지정`, CATEGORY SCREEN, FAVORITE SCREEN
    - 이유: source row가 리스트 흐름에서 빠지며 주변 row가 자연스럽게 압축되고, 시스템 reorder 안정성/접근성을 활용할 수 있다.
- 카테고리 그룹 간 이동이 필요한 화면은 custom drag engine을 사용한다.
    - 예: TODO SCREEN `카테고리별 순서`, ALL TODOS SCREEN
    - 이유: cross-category move, 접힌 카테고리 hover auto-expand, 같은 gesture 유지, category header reorder, Inbox pinned drop constraint가 필요하다.
- 따라서 `NativeManagedList` facade와 저장 contract는 공유하지만, iOS 내부 reorder 엔진은 화면 구조에 따라 다를 수 있다.

## 디자인

### 일정 완료 / 완료취소 토글 버튼
- ios + android 공통으로 쓸 수 있는 커스텀 토글 버튼 디자인을 사용한다.

### 일정 List Item
    - ios
        - 왼쪽: 일정 완료 / 완료취소 토글 버튼 + 라벨 (완료된 일정은 label dim 처리)
        - 오른쪽: 없음 / 즐겨찾기 추가 / 해제 토글 버튼 별 filled / outline 아이콘 (즐겨찾기 스크린에서만 사용)
    - android
        - 왼쪽: 일정 완료 / 완료취소 토글 버튼 + 라벨 (완료된 일정은 label dim 처리)
        - 오른쪽: 즐겨찾기 추가 / 해제 토글 버튼 별 filled / outline 아이콘 (즐겨찾기 스크린에서만 사용) + ⋮ 아이콘

### 카테고리 List Item
    - ios
        - 왼쪽: 카테고리 색상 아이콘 / 라벨
        - 오른쪽: 카테고리 안에 있는 일정의 갯수 + 오른쪽 arrow icon
    - android
        - 왼쪽: 카테고리 색상 아이콘 / 라벨
        - 오른쪽: ⋮ 아이콘

### 일정 List Item Sub Label 표시
 - ios + android
    - Star 아이콘 + 즐겨찾기 추가 된 일정: 즐겨찾기 표시 // 예외: 즐겨찾기 스크린에서는 표시 안함.
    - Calendar Today 아이콘 + 일반일정: 일정의 마감일 / 기간이 있는 일정: 일정의 기간 (표시: 시작일 - 마감일) -> 마감일이 지난 일정은 Warning 아이콘으로 바뀌고, 기간이 지난 일정이라고 표시한다. (표시: 시작일 - 마감일 (기간이 지난 일정))
    - Alarm 아이콘 + 시간이 있는 일정: 일정의 시간 (표시: 시작시간 - 종료시간)
    - Cached 아이콘 + 반복 일정: 반복 설정 텍스트를 표시한다. (예: 매주 금요일, 매달 15일, 매일) // 예외: TODO SCREEN 상단 즐겨찾기 그룹에서는 다음 일정 표시 가능

### 카테고리 List Item Sub Label 표시
 - ios
    - 표시안함
- android
    - 표시함 (n개의 일정)

## listItem 메뉴 구성

### 일정 List Item
- 일정 (보기), 수정, 이동, 즐겨찾기 추가 / 해제, 일정 복사, 일정 삭제
- 오른쪽에서 왼쪽으로 스와이프 - 삭제
- 왼쪽에서 오른쪽으로 스와이프 - 즐겨찾기 추가 / 해제

### 카테고리 List Item
- 수정, 삭제
- 오른쪽에서 왼쪽으로 스와이프 - 삭제


## 일정 listItem 반복일정 완료 및 표시 규칙 (일정을 기간에 관계없이 항상 보여줘야 되어야 하는 스크린에서 사용 (TODO SCREEN - FAVORITE 부분, FAVORITE SCREEN, CATEGORY SCREEN))
- 완료 규칙
    - 오늘 날짜를 기준으로 오늘을 포함한 가까운 날짜를 우선적으로 완료 처리한다.
    - 완료 처리하면 사라지지 않고, label dim 처리한다.

- 표시 규칙
    - 오늘 날짜를 기준으로 오늘을 포함한 가까운 날짜를 우선적으로 표시
    - 다음 일정은 표시하지 않는다. 매일 12시 마다 오늘 날짜를 기준으로 반복 일정을 계산해서 표시 해준다. 다음 반복일정은 다음날 표시된다. 그리고 만약 매주 목요일 반복 일정이 있었을떄, 오늘이 화요일일때, 완료 처리하면 다음주 목요일이 지나고 금요일이 되어야 다음 일정이 표시된다.
일반, 기간이 있는 일정 이런 것은 완료 처리가 쉽지만, 반복 일정의 경우에는 오늘 날짜를 기준으로 오늘을 포함한 가까운 날짜를 우선적으로 완료 시키고 다른 일정과 마찬가지고 label dim 처리한다. 그리고 다음 일정은 표시하지 않는다. 매일 12시 마다 오늘 날짜를 기준으로 반복 일정을 계산해서 표시 되니까 만약 오늘을 포함한 매일 반복일정이 있었고, 오늘 카테고리에 속한 일정 페이지에서 완료 처리했을때, 다음 반복일정은 다음날 표시된다. 그리고 만약 매주 목요일 반복 일정이 있었을떄, 오늘이 화요일일때, 완료 처리하면 다음주 목요일이 지나고 금요일이 되어야 다음 일정이 표시된다.



## CATEGORY (MY PAGE)
- group title 있음 (카테고리)
- my page 화면 안에 있음.
- 카테고리 listItem 사용

## CATEGORY SCREEN
- 카테고리에 속한 일정들이 들어가는 screen
- my page에서 카테고리 listItem 클릭하면 해당 screen으로 이동한다.
- 해당 카테고리에 속한 일정들이 이 screen에 표시된다.
- 일정 listItem 사용

### 일정 완료 시 규칙
- 일정 완료 버튼으로 완료/미완료 토글 가능
- 완료 처리하면 사라지지 않고, label dim 처리한다.

### 순서
- 카테고리 내부 일정 순서를 사용한다.
- 사용자가 순서를 변경 하면 카테고리 내부 일정 순서가 반영되도록 저장되어야 한다.

### reorder
- 사용

### 다른 카테고리 이동
- 여기서는 다른 카테고리가 화면에 보이지 않기 때문에 drag 해서 다른 카테고리로 이동시키지 않는다.
- 메뉴의 `이동`으로만 다른 카테고리로 이동 가능하다.
- 메뉴의 `이동`은 카테고리 변경과 함께 대상 카테고리 맨 아래 append 로 처리한다.
- 정확한 위치 조정은 reorder 가능한 화면에서 처리한다.

## TODO SCREEN
오늘 또는 selected된 날짜에 속한 일정들이 들어가는 스크린

### 일정 완료 시 규칙
- 일정 완료 버튼으로 완료/미완료 토글 가능
- 완료된 일정은 사라짐 (즐겨찾기는 예외. 아래 참조)

### 일정 탭 동작
- 일정 클릭 시 아래에서 일정 상세정보가 bottom sheet 형태로 올라온다.

### 헤더 메뉴
- 헤더 부분 오른쪽 ⋮ 아이콘을 누르면 정렬을 변경할 수 있다.
- 마지막으로 선택한 정렬 모드(`시간순 / 사용자 지정 / 카테고리별 순서`)는 로컬에 저장하고, 다음 진입 시 그대로 복원한다.

### 순서
- 시간순, 사용자 지정, 카테고리별 순서
- 즐겨찾기는 무조건 맨위 고정.
    - 즐겨찾기에 등록된 일정은 날짜에 상관없이 항상 보여준다.
    - 반복 일정의 경우, 카테고리안의 일정에서 정해놓은 규칙에 따라 다음 반복일정을 계산해서 보여준다. (카테고리안의 일정 규칙: 반복 일정이라면, 오늘 날짜를 기준으로 오늘을 포함한 가까운 날짜를 우선적으로 보여준다. 다음 일정은 표시하지 않는다. 매일 12시 마다 오늘 날짜를 기준으로 반복 일정을 계산해서 표시 해준다. 다음 반복일정은 다음날 표시된다. 그리고 만약 매주 목요일 반복 일정이 있었을떄, 오늘이 화요일일때, 완료 처리하면 다음주 목요일이 지나고 금요일이 되어야 다음 일정이 표시된다.)
    - TODO SCREEN 상단 즐겨찾기 그룹의 반복 일정 sub label 은 반복 설정 텍스트 대신 `다음 일정` 기준으로 표시 가능하다. (예: 다음 일정 5월 3일, 다음 일정 내일)

    - 즐겨찾기에 등록된 일정은 아래 목록들에서는 안보여준다.
    - 즐겨찾기에 등록된 일정은 고정되어 계속 해서 추적하는 일정이기 때문에 완료 처리 시 목록에서 사라지지 않고, label dim 처리한다. (아래 추가사항의 중요 2 항목 참조)

1. 시간순
    - 시작일 기준 일정의 시간 순서
    - 시작일이 같다면, 생성된 시간 순서
    - 이 모드에서는 일정 reorder 불가
    - 1. 년 반복 일정
    - 2. 기간 일정
    - 3. 오늘 일정 (하루종일)
    - 4. 월 반복 일정
    - 5. 주간 반복일정
    - 6. 매일 반복 일정
    - 7. 시간이 있는 일정 & 기간과 시간이 있는 일정 (시간순 ⬇️)

2. 사용자 지정
    - selected된 날짜에 속한 일정을 사용자가 직접 정한 순서대로 표시
    - 이 모드의 순서는 todo 원본의 `custom order`로 저장한다.
    - TODO SCREEN 상단 구조는 `캘린더 위젯 -> 즐겨찾기 그룹 -> 일반 일정 목록` 순서로 고정한다.
    - 즐겨찾기 그룹은 맨 위에 고정되며, 접기 / 펼치기 가능하다.
    - 일반 일정은 카테고리 그룹 없이 하나의 flat list 로 표시한다.
    - 이 모드에서는 일정 reorder 허용
    - 이 모드에서는 다른 카테고리로 drag 이동은 허용하지 않는다.
    - 카테고리 변경은 메뉴의 `이동`으로만 처리한다.
    - selected된 날짜 필터에 새로 걸려 보이게 되는 반복 일정 / 기간 일정도 기존 todo 원본의 `custom order`를 그대로 따른다.
    - 새 todo 생성 시에는 전체 custom order 맨 아래로 추가된다.

3. 카테고리별 순서
    - 카테고리의 순서와 카테고리 안에 있는 일정의 순서대로 표시 (selected된 날짜 기준)
    - 이 모드는 `ALL TODOS SCREEN`과 같은 category-grouped interaction model을 재사용하되, selected된 날짜에 보이는 일정만 각 카테고리 아래에 표시한다.
    - 카테고리별 순서로 볼 때는 오늘 일정이 없어도 모든 카테고리 group title 을 표시한다.
    - 카테고리 group header 오른쪽 아이콘을 누르면 해당 카테고리 그룹이 접히거나 펼쳐진다.
    - 카테고리별 순서로 볼 때는 일정 reorder 허용
    - 카테고리별 순서로 볼 때는 꾹 눌러서 다른 카테고리로도 이동 가능. (이동한 일정은 카테고리 일정 페이지에서 순서가 반영됨)
    - 다른 카테고리로 이동할 때도 대상 카테고리 안의 일정 사이사이로 드롭 가능해야 한다.
    - 다른 카테고리로 이동하면 대상 카테고리의 보이는 위치 기준으로 삽입된다.
    - ios 에서는 카테고리 group header 를 꾹 누르면, 해당 카테고리에 속한 일정들이 접힌 상태로 카테고리 reorder 가능
    - android 에서는 카테고리 group header 를 꾹 누르면 카테고리 reorder 가능
    - 카테고리 reorder 시에는 카테고리 순서만 바뀌고, 카테고리 안의 일정 순서는 유지된다.
    - Inbox 같은 system category는 최상단 고정이며, 다른 카테고리를 그 위로 이동할 수 없다.
    - 접힌 카테고리에 drag hover 가 일정 시간(약 0.5초 전후) 유지되면 해당 카테고리가 자동으로 펼쳐진다.
    - 자동으로 펼쳐진 뒤에는 해당 카테고리 내부의 보이는 gap 위치에 그대로 drop 가능해야 한다.
    - 카테고리별 순서로 볼 때는 즐겨찾기 그룹으로 드롭 가능
    - 즐겨찾기 해제 시에는 원래 숨겨놨던 일반 일정 목록이 다시 보이게 한다.

### 추가 사항
- 해당 스크린의 일정 목록은 기본적으로 Plain List이지만, Inset Grouped List 처럼 그룹명이 들어간다. 즐겨찾기 / 기본일정들은 selected된 날짜로 (오늘, 내일, 모레, 특정 날짜 등) 그룹명이 표시된다.
- 즐겨찾기 추가 된 일정 목록은 항상 맨위에 고정.
- 중요 1. 일정 순서 변경에서 일정을 끌어서 이동 시킬 때, 즐겨찾기 안으로도 넣을수 있음. (이동한 일정은 즐겨찾기 목록에 추가됨), 그리고 즐겨찾기로 추가 시킬때, 순서도 사용자가 옮긴 순서가 반영됨.
- 중요 2. 즐겨찾기에 등록된 일정은 고정되어 계속 해서 보여주는 일정이기 때문에 완료 처리 시 목록에서 사라지지 않고, label dim 처리한다. (이건 즐겨찾기 스크린에서도 동일하게 적용되어야함) 만약 반복 일정이라면, 앞서 카테고리안의 일정에서 정해놓은 규칙대로 오늘 날짜를 기준으로 오늘을 포함한 가까운 날짜를 우선적으로 완료 시키고 다른 일정과 마찬가지고 label dim 처리한다. 그리고 다음 일정은 표시하지 않는다. 매일 12시 마다 오늘 날짜를 기준으로 반복 일정을 계산해서 표시 되니까 만약 오늘을 포함한 매일 반복일정이 있었고, 오늘 카테고리에 속한 일정 페이지에서 완료 처리했을때, 다음 반복일정은 다음날 표시된다. 그리고 만약 매주 목요일 반복 일정이 있었을떄, 오늘이 화요일일때, 완료 처리하면 다음주 목요일이 지나고 금요일이 되어야 다음 일정이 표시된다.


## ALL TODOS SCREEN
날짜 제한 없이 전체 일정이 들어가는 스크린

### 목적
- 전체 일정을 카테고리 기준으로 한눈에 보고 관리하는 화면
- TODO SCREEN 처럼 selected된 날짜 기준으로 보여주는 화면이 아니라, 날짜와 관계없이 전체 일정을 보여주는 화면
- 카테고리 / 일정의 순서를 구조적으로 관리하는 화면

### 리스트 구조
- 카테고리별 그룹으로 일정이 묶여서 표시된다.
- `TODO SCREEN > 카테고리별 순서`와 같은 category-grouped interaction model을 재사용하되, 이 화면은 selected된 날짜 필터 없이 전체 일정을 넣는다.
- 각 카테고리 그룹 아래에 해당 카테고리에 속한 일정 listItem 들이 표시된다.
- 카테고리 그룹 title은 카테고리 listItem 스타일을 기준으로 사용한다.
- 일정은 일정 listItem 스타일을 사용한다.
- 반복 일정도 원본 일정 1개만 표시한다. occurrence를 날짜별로 펼쳐서 여러 줄로 표시하지 않는다.
- 반복 일정의 sub label은 가까운 날짜 계산이 아니라 반복 설정 텍스트를 표시한다. (예: 매주 금요일, 매달 15일, 매일)

### 카테고리 그룹 헤더
- 왼쪽: 카테고리 색상 아이콘 / 카테고리명
- 오른쪽: 위 / 아래 아이콘
- 오른쪽 아이콘을 누르면 해당 카테고리 그룹이 접히거나 펼쳐진다.
- 카테고리 그룹이 접히면 해당 카테고리에 속한 일정 listItem 들은 보이지 않는다.

### 카테고리 그룹 헤더 인터랙션
- ios
    - 카테고리 그룹 헤더를 꾹 누르면, 해당 카테고리에 속한 일정들이 접힌 상태로 카테고리 reorder 가능
- android
    - 카테고리 그룹 헤더를 꾹 누르면 카테고리 reorder 가능
- 카테고리 reorder 시에는 카테고리 순서만 바뀌고, 카테고리 안의 일정 순서는 유지된다.
- Inbox 같은 system category는 최상단 고정이며, 다른 카테고리를 그 위로 이동할 수 없다.

### 일정 listItem 인터랙션
- ios
    - 꾹 누르면 커스텀 네이티브 메뉴가 나오고, 그 상태에서 움직이면 reorder 가능
- android
    - 꾹 누르면 reorder 가능
    - ⋮ 아이콘 클릭시 하단에서 액션 메뉴가 올라옴
- 일정 listItem 는 같은 카테고리 안에서 순서 변경 가능
- 일정 listItem 를 다른 카테고리 그룹으로 이동 가능
- 다른 카테고리 그룹으로 이동하면 categoryId 와 순서가 같이 변경된다.
- 다른 카테고리 그룹으로 이동할 때도 대상 카테고리 안의 일정 사이사이로 드롭 가능해야 한다.
- 다른 카테고리 그룹으로 이동하면 대상 카테고리의 보이는 위치 기준으로 삽입된다.

### 일정 listItem 메뉴 구성
- 공통 일정 List Item 메뉴 구성을 사용한다.
- 즐겨찾기 항목은 일정 상태에 따라 `즐겨찾기 추가` 또는 `즐겨찾기 해제`로 표시된다.
- 오른쪽에서 왼쪽으로 스와이프 - 삭제
- 왼쪽에서 오른쪽으로 스와이프 - 즐겨찾기 추가 / 해제

### 접힘 / 펼침 규칙
- 접힘 상태는 카테고리별로 저장되어야 한다.
- 접힌 카테고리에도 다른 일정 listItem 를 드롭할 수 있다.
- 접힌 카테고리에 drag hover 가 일정 시간(약 0.5초 전후) 유지되면 해당 카테고리가 자동으로 펼쳐진다.
- 자동으로 펼쳐진 뒤에는 해당 카테고리 내부의 보이는 gap 위치에 그대로 drop 가능해야 한다.
- 접힌 카테고리 안의 일정은 화면에서 보이지 않지만, 내부 순서 데이터는 그대로 유지된다.

### 순서 규칙
- 카테고리 순서는 My Page 의 카테고리 순서를 기본으로 따른다.
- 카테고리 안 일정의 순서는 CATEGORY SCREEN 에서 사용하는 카테고리 내부 순서를 기본으로 따른다.
- 사용자가 ALL TODOS SCREEN 에서 카테고리 순서를 바꾸면, My Page 카테고리 순서에도 반영된다.
- 사용자가 ALL TODOS SCREEN 에서 일정 순서를 바꾸거나 다른 카테고리로 이동시키면, CATEGORY SCREEN 에도 반영된다.

### 일정 완료 시 규칙
- 일정 완료 버튼으로 완료/미완료 토글 가능
- 이 화면은 전체 보기만 사용한다.
- 완료 처리하면 사라지지 않고, label dim 처리한다.
- 반복 일정은 CATEGORY SCREEN / FAVORITE SCREEN 에서 사용하는 반복 일정 완료 규칙과 동일하게 처리한다.

### 추가 사항
- 이 화면은 TODO SCREEN 의 날짜 중심 화면과 다르므로, selected된 날짜 개념을 사용하지 않는다.
- 이 화면은 전체 구조 관리용 화면이므로, 카테고리 reorder 와 일정 reorder 둘 다 지원한다.
- 즐겨찾기 그룹은 별도 상단 그룹으로 분리하지 않는다.
- 즐겨찾기 추가 된 일정은 `일정 List Item Sub Label 표시` 규칙의 Star 아이콘 + 즐겨찾기 표시를 사용한다.


## FAVORITE SCREEN
즐겨찾기 일정들이 들어가는 스크린

### 일정 완료 시 규칙
- 일정 완료 버튼으로 완료/미완료 토글 가능
- 완료된 일정은 label dim 처리
- 일반, 기간이 있는 일정 이런 것은 완료 처리가 쉽지만, 반복 일정의 경우에는 오늘 날짜를 기준으로 오늘을 포함한 가까운 날짜를 우선적으로 완료 시키고 다른 일정과 마찬가지고 label dim 처리한다. 그리고 다음 일정은 표시하지 않는다. 매일 12시 마다 오늘 날짜를 기준으로 반복 일정을 계산해서 표시 되니까 만약 오늘을 포함한 매일 반복일정이 있었고, 오늘 카테고리에 속한 일정 페이지에서 완료 처리했을때, 다음 반복일정은 다음날 표시된다. 그리고 만약 매주 목요일 반복 일정이 있었을떄, 오늘이 화요일일때, 완료 처리하면 다음주 목요일이 지나고 금요일이 되어야 다음 일정이 표시된다.

#### 순서
- 기본은 즐겨찾기 추가된 순서 (오래된 순)
- 사용자가 순서를 변경 하면 favorite 전용 order 가 업데이트 되도록 저장되어야 한다.

#### reorder
- 허용
- favorite 전용 order 를 따로 만들어서 관리하는 방향으로 한다.

#### 일정 listItem 메뉴 구성
- 공통 일정 List Item 메뉴 구성을 사용한다.
- 이 화면에서는 모든 일정이 즐겨찾기 상태이므로 메뉴에는 `즐겨찾기 해제`로 표시된다.
- 오른쪽에서 왼쪽으로 스와이프 - 삭제
- 왼쪽에서 오른쪽으로 스와이프 - 즐겨찾기 해제

#### 추가 사항
- 즐겨찾기 추가/해제 토글 버튼을 클릭하면 즐겨찾기 추가/해제 되면서 별 아이콘 색깔이 변한다.
- 즐겨찾기 스크린에서는 즐겨찾기 해제시 해당 일정은 즐겨찾기 목록에서 바로 사라지지 않는다. 일정 목록을 새로고침 해야 사라진다. (유저 ux 편의성)
